// AdventureMS Lime Balloon Reactor

function act()
{
    // Call the guide
    rm.openNpc(9900001);

    // Message the player
    // rm.getPlayer().yellowMessage("The ruins accept your offering..."); // Yellow Player Message
}